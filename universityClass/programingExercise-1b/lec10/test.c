# include <stdio.h>
# include <math.h>

char test(int n) {
    int n, size;
    size = pow(3, n);
}

int main(void) {
    int n, size;
    scanf("%d", &n);
    size = pow(3, n);
    char list[size][size];


}
